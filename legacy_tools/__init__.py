"""
Legacy tool implementations — individual descriptor functions.

These are the original tools from Intern-S1-recipe. They are preserved for
backward compatibility. New code should use the consolidated tools in the
parent package.

Imports are lazy to avoid pulling in heavy/optional dependencies
(pydantic_ai, dimorphite_dl, etc.) unless actually needed.
"""
